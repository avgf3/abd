#!/bin/bash

# اختبار إصلاح مشكلة الملف الشخصي

echo "🧪 اختبار إصلاح مشكلة الملف الشخصي..."

# التحقق من وجود الخادم
echo "🔍 التحقق من حالة الخادم..."
if curl -s http://localhost:3000/health > /dev/null; then
    echo "✅ الخادم يعمل على المنفذ 3000"
else
    echo "❌ الخادم غير متاح على المنفذ 3000"
    echo "💡 يرجى تشغيل الخادم أولاً: npm run dev"
    exit 1
fi

# اختبار API
echo "🔍 اختبار API..."
if curl -s http://localhost:3000/api/health > /dev/null; then
    echo "✅ API يعمل بشكل صحيح"
else
    echo "❌ API غير متاح"
    exit 1
fi

# اختبار قاعدة البيانات
echo "🔍 اختبار قاعدة البيانات..."
if curl -s -X GET http://localhost:3000/api/users/1 > /dev/null; then
    echo "✅ قاعدة البيانات تعمل بشكل صحيح"
else
    echo "❌ مشكلة في قاعدة البيانات"
    exit 1
fi

echo "✅ جميع الاختبارات نجحت!"
echo "🎉 الإصلاح يعمل بشكل صحيح"

# نصائح للاختبار اليدوي
echo ""
echo "📋 نصائح للاختبار اليدوي:"
echo "1. افتح الملف الشخصي لأي مستخدم"
echo "2. تأكد من ظهور الصورة بشكل صحيح"
echo "3. تأكد من ظهور آخر تواجد بشكل صحيح"
echo "4. تأكد من ظهور الغرفة الحالية بشكل صحيح"
echo "5. انتقل لغرفة أخرى وتأكد من تحديث الغرفة الحالية"
echo "6. أعد تحميل الصفحة وتأكد من استمرارية البيانات"