#!/bin/bash

echo "🚀 بدء تشغيل موقع الدردشة العربية - الإنتاج"
echo "=============================================="

# التأكد من وجود node_modules
if [ ! -d "node_modules" ]; then
  echo "📦 تثبيت المتطلبات..."
  npm install
fi

# بناء المشروع للإنتاج
echo "🔨 بناء المشروع للإنتاج..."
npm run build

# تشغيل الخادم في وضع الإنتاج
echo "🏭 تشغيل الخادم في وضع الإنتاج..."
NODE_ENV=production npm start

echo "✅ تم تشغيل الموقع في وضع الإنتاج!"
echo "=============================================="