import FinalGiftSelection from '@/components/chat/FinalGiftSelection';

export default function FinalSelectionPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-gray-100 via-blue-50 to-purple-100 py-8">
      <FinalGiftSelection />
    </div>
  );
}