# Server
NODE_ENV=development
PORT=5000

# Database
# PostgreSQL connection string, e.g.:
# postgresql://USER:PASSWORD@HOST:5432/DB_NAME?sslmode=require
DATABASE_URL=

# Session/Authentication (if applicable)
SESSION_SECRET=replace-with-a-strong-secret

# Client build/runtime public envs (Vite exposes only prefixes VITE_/PUBLIC_)
VITE_API_BASE=/api
PUBLIC_APP_NAME=Chat App

# Optional Render/Replit detection
REPL_ID=